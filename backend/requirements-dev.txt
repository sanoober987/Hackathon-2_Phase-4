# ── Development / test dependencies (not installed in production) ──────────────
-r requirements.txt

aiosqlite==0.20.0       # SQLite async driver for local dev/tests
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1           # async test client for FastAPI
